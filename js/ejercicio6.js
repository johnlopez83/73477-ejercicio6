const inputData = +prompt("Ingrese la cantidad de filas")


for(let i = inputData -1; i >=0; i-- ){
    
    for(let j=0; j<=i; j++){
        console.log('*')
        document.write('*')
    }
        document.write('<br>')
        console.log('\n')
}

